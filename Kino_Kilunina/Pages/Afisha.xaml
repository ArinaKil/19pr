<Page x:Class="Kino_Kilunina.Pages.Afisha"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Kino_Kilunina.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Афиша">

    <Grid Background="#F4F6F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="0 0 0 1">
            <Grid Margin="16 0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Афиша" FontSize="20" FontWeight="Bold"
                               Foreground="#1A1A2E" VerticalAlignment="Center"/>
                    <Button Content="Кинотеатры" Margin="16 0 0 0"
                            Width="110" Height="34" FontSize="13"
                            Background="#F4F6F9" Foreground="#4A90D9"
                            BorderBrush="#4A90D9" BorderThickness="1" Cursor="Hand"
                            Click="btnKino_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
                <Button Content="+ Добавить" HorizontalAlignment="Right"
                        Width="110" Height="34" FontSize="13"
                        Background="#4A90D9" Foreground="White"
                        BorderThickness="0" Cursor="Hand"
                        Click="btnAdd_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="wpAfisha" Margin="8" Orientation="Horizontal"/>
        </ScrollViewer>
    </Grid>
</Page>
